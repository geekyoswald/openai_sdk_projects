# ComplAI SDR

1. Copy `.env.example` to `.env` and fill in the keys.
2. `pip install -r requirements.txt`
3. `python run.py`

The email brief is hardcoded in `run.py` (`BRIEF`). On AWS, do the same on the instance (venv optional) and run `python run.py`.
